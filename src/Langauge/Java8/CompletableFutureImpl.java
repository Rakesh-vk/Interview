package Langauge.Java8;
/*
CompletableFuture<T> is a class introduced in Java 8 that represents a future result of an asynchronous computation. It allows you to write non-blocking, async pipelines — you kick off a task, chain what should happen next, and the thread is free to do other work meanwhile.
Think of it like ordering food at a restaurant — you place the order (async task), get a token (CompletableFuture), and you're free to do other things. When food is ready, the waiter (callback) brings it to you.


*/
public class CompletableFutureImpl {
    public static void main(String[] args) {

    }
}
